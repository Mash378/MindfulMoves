# MindfulMoves

## Steps to get the frontend running:

1. First go to terminal and use: cd frontend
2. To install dependencies use: npm install
3. To run the Vite dev server go to the terminal and use: npm run dev
4. To open the webpage click on: http://localhost:5173/